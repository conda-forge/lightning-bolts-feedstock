{% set name = "lightning-bolts" %}
{% set version = "0.6.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/lightning-bolts-{{ version }}.tar.gz
  sha256: d52cd197fd848794138577b44569b4f209c3bc0ffa389808b938a9a141479ed4

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  noarch: python

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - dataclasses
    - packaging
    - python >=3.6
    - pytorch-lightning >=1.7.0
    - pytorch >=1.6
    - torchmetrics >=0.4.1
    - lightning-utilities
    - wandb
    - pillow
    - torchvision
    - scikit-learn
    - matplotlib-base

test:
  imports:
    - pl_bolts
    - pl_bolts.callbacks
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/Lightning-AI/lightning-bolts
  summary: PyTorch Lightning Bolts is a community contribution for ML researchers.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - Borda
    - Tobias-Fischer
